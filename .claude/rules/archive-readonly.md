# C5: archive/ 수정 금지

- `archive/foodtech-hub-deploy/`는 참고용 프로토타입이다. 어떤 경우에도 수정하지 않는다.
- 로직 참고(뉴스 수집, CSV 임포트 등)와 결함 반면교사 용도로만 읽는다.
- 프로토타입 코드를 가져올 때는 복사가 아니라 신규 코드 컨벤션(route→service→model)에 맞게 재작성한다.
