# 분류 프롬프트 개선 실측 — 희정 검수 코퍼스 재채점 (2026-07-21)

검수 완료본(`뉴스분류_검수완료.md`)을 정답지로, 7/18 캐시 80건을 재분류해 프롬프트·모델을 비교했다.
평가 스크립트는 세션 스크래치패드(일회성) — 기준: 폐기 대상 15건(위키2·야후는 코드 차단이라 제외),
일반 이동 6건, 유지 53건의 과교정 여부.

## 결과

| 구성 | 폐기 감지 | 일반 판정 | 과교정(정상 기사 폐기) |
|---|---|---|---|
| flash + v1 (규칙 5줄, 어제 커밋) | 3/15 | 0/6 | 3 |
| **flash + v2 (reason 강제 + few-shot)** | **8/15** | **1/6** | **3** |
| haiku-4.5 + v2 | 10/15 | 2/6 | **12** ← 영양·식단 정상 기사까지 폐기 |
| gemini-2.5-pro + v2 | 무효 | 무효 | 무효 (reasoning 토큰이 출력 잘라먹음 — T-004 "reasoning 모델 제외" 재확인) |

### 재채점 (요약 300자 반영 후, 같은 날)

flash + v2 + 요약 300자: 폐기 8/15 · 일반 1/6 · 실질 과교정 3 — 총점은 같지만 구성이 좋아짐.
**명백한 무관 기사(의료AI·신약·자동차IT·치과3D·퀴즈×2·복지·군정)는 전부 잡는다.**
남은 미스는 전부 경계 케이스: 외식-인접(레스토랑 소개·축제·승계 기사 → 외식 푸드테크),
주제-인접(ESG 콘텐츠 → 업사이클링/친환경포장), 기업 홍보성(AFPRO 부스 기사).
실행 간 변동도 확인됨(temperature) — 같은 구성이 회차별로 ±2건 다르게 나옴.
※ 이날 1회 실행에서 배치 3개가 통째로 무응답(일시적 API 빈 응답) — 운영에선 미저장분이
다음 크론에서 재시도되는 설계라 자기치유되지만, 통계 해석 시 알아둘 것.

## 판정: flash + v2 채택

- v2 개선 장치: ① reason(기사의 실제 주제 한 문장)을 category보다 먼저 출력하게 강제
  ② "키워드가 보여도 주제가 아니면 폐기" 명시 ③ 코퍼스 밖 가상 예시 4개(few-shot).
  검수 문항을 예시로 넣지 않은 이유 = 평가 오염 방지.
- haiku-4.5는 감지력이 낫지만 과교정 12건으로 실격 — 뉴스레터 재료를 너무 죽인다.
- 남은 미스 패턴: 사명 앵커링("한화푸드테크" → 외식), 요약 속 분야 키워드(ESG·업사이클링 언급).
  제목+요약 200자 입력의 구조적 한계로 판단.

## 운영 함의

- DB 유입 노이즈 추정: 검수 시점 32%(25/78) → v2 약 13%(10/77). 0%가 목표가 아니라
  **발송 조립(T-008)에서 사람이 최종 선별**하는 전제로 충분한 수준.
- 이 평가는 재실행 가능(캐시 코퍼스 + 검수 정답지) — 프롬프트를 만질 때마다 재채점하고
  감으로 판단하지 않는다. 다음 검수 라운드가 오면 정답지를 갱신한다.
- "농식품부 제도 → 해당없음"처럼 희정 판정(일반)과 갈리지만 방어 가능한 경계 케이스 존재 —
  순수 오답과 구분해서 볼 것.
