# 7/23(목) 16:30 싱크 — 안건 메모 (사전 정리)

데모: `GET /admin/status` 현황판 라이브 (admin / ADMIN_TOKEN) + 푸디픽 #0 실물(민겸 수신함).
전달: 3주차에 파이프라인 ①③④⑤ 운영 가동 완료, 남은 코드 조각은 Activity Score 하나.

## 결정 필요 안건 4건

### 1. 파일럿 발송 시점·운영 방식
- 상태: 발송 코드 완성 + 실발송 검증 완료. 랩실 명단만 오면 `pilot-lab` 임포트 → 발송 가능.
- 결정할 것: ① 첫 발송일(목요일 정기? 명단 수령 즉시 1회 테스트?) ② 발송 전 초안 확인자(민겸? 희정?)
  ③ 랩실 명단 전달 방법·시점 (교수님이 수집 예정)
- 추천: 명단 수령 → T-009 큐레이션 반영 → 다음 목요일부터 매주 정기. 초안은 발송 전날 확인.

### 2. 디저트 코너(행사 CTA) "(광고)" 표기 — 법무 확인
- 배경: 정보통신망법상 영리 목적 광고성 정보는 제목에 "(광고)" 표기 의무. 행사 홍보 CTA가
  해당하는지 애매(결정 11에서 이월). 파일럿은 디저트 코너 제외로 회피 중.
- 결정할 것: 학교/협의회 행사 안내가 "영리 광고"에 해당하는지 법무 자문 경로 (기관 법무? 외부?)
- 트레이드오프: "(광고)" 붙이면 오픈율 하락 예상 ↔ 미표기 강행은 과태료 위험(최대 3천만 원).
  회피안: 행사 안내를 별도 명시 동의 기반 메일로 분리하거나, 비영리성 명확한 행사만 게재.

### 3. Resend Pro($20/월) 전환 시점
- 현재: 무료 티어 일 100통. 파일럿(랩실, 100명 이하)은 무료로 충분.
- 전환 트리거: 세그먼트 100명 초과 본 발송 시작 직전. 3,000명 전체면 월간 발송량 1.2만+ 통.
- 결정할 것: 결제 수단·계정 주체(현재 Resend 계정 소유 확인) — 비용 전망 노션 cost 페이지 갱신.

### 4. 엑셀 → 구글시트 이관 (회원 명단 운영 루틴)
- 현재: 실명단 엑셀(시트 19장) → 임포터로 Supabase 3,413명. 워크북 원본 시트 확정 안 됨.
- 결정할 것: ① 직원 편집용 구글시트 정본 지정(어느 시트가 마스터인지) ② 갱신 주기(월 1회?)
  ③ 운영 루틴 = 시트 수정 → 엑셀 다운로드 → `scripts/import_members.py --dry-run` → 적용
- 후속: 구글시트 API 직접 연동은 별도 티켓(수동 루틴이 자리 잡은 뒤).

## 공유 사항 (결정 불필요)

- 분류 품질: 희정 검수 25건 전부 반영, 명백 노이즈는 전부 차단(실측 문서 prompt-eval-2026-07-21.md).
  검수 감사 인사 + "다음 검수 라운드는 파일럿 후 새 정답지로" 제안.
- 뉴스레터 디자인: 파란 팔레트 적용 완료 — 최종 컨펌 요청 (미리보기 지참).
- 비용 현황: Railway $5/월 + LLM 분류 <$1.5/월 + 도메인. Resend Pro 전환 전까지 변동 없음.
