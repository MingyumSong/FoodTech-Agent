version: "3.9"
services:
  app:
    build: .
    container_name: foodtech-hub
    ports:
      - "8000:8000"
    environment:
      - DART_API_KEY=${DART_API_KEY:-}
      - BRAVE_API_KEY=${BRAVE_API_KEY:-}
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
